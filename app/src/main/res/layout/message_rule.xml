<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.AppCompatTextView
    android:id="@+id/tvRule"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:contentDescription="Selection rule: at least one Guest in the party must have a reservation. Guests without reservations must remain in the same booking party in order to enter."
    android:drawableStart="@drawable/ic_info"
    android:drawablePadding="5dp"
    android:paddingHorizontal="10dp"
    android:text="Selection rule: at least one Guest in the party must have a reservation. Guests without reservations must remain in the same booking party in order to enter."
    android:textColor="@color/disney_main_color"
    android:textSize="14sp"
    xmlns:android="http://schemas.android.com/apk/res/android"
    layout_height=""/>